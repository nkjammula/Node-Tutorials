const log = require("./logger");

// function sayHello(name) {
//   console.log("hello " + name);
// }
// sayHello("naveen");
// // window object is not in node, it is only for browser.
// // below code gives error
// // console.log(window);
// var message = "";
// console.log(global);
// each module is a json object with properties for that file.
// log("hello new message");
